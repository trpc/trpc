{
  "name": "examples-standalone-server",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^22.20.0"
  },
  "scripts": {
    "dev:server": "node --watch src/server.ts",
    "dev:client": "wait-port 2022 && node --watch src/client.ts",
    "dev": "run-p dev:* --print-label",
    "start": "pnpm dev",
    "build": "esbuild src/server.ts src/client.ts --bundle --packages=external --platform=node --format=esm --outdir=dist --sourcemap",
    "lint": "eslint --cache src",
    "typecheck": "tsc",
    "test-dev": "start-server-and-test 'node src/server.ts' 2022 'node src/client.ts'",
    "test-start": "start-server-and-test 'node dist/server.js' 2022 'node dist/client.js'"
  },
  "dependencies": {
    "@trpc/client": "npm:@trpc/client",
    "@trpc/react-query": "npm:@trpc/react-query",
    "@trpc/server": "npm:@trpc/server",
    "ws": "^8.0.0",
    "zod": "^3.25.51"
  },
  "alias": {
    "scheduler/tracing": "../../node_modules/scheduler/tracing-profiling"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "@types/ws": "^8.2.0",
    "esbuild": "^0.17.10",
    "eslint": "^9.26.0",
    "npm-run-all": "^4.1.5",
    "start-server-and-test": "^1.12.0",
    "typescript": "^5.9.2",
    "wait-port": "^1.0.1"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "version": "11.7.2"
}
