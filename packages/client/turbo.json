{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [
        "package.json",
        "dist/**",
        "links/**",
        "unstable-internals/**"
      ]
    }
  }
}
