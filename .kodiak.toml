# .kodiak.toml
version = 1

[merge]
method = "squash"
automerge_label = "🚀 merge"

[approve]
auto_approve_usernames = ["dependabot", "KATT"]

[merge.automerge_dependencies]
# only auto merge "minor" and "patch" version upgrades.
versions = ["minor", "patch"]
usernames = ["dependabot"]

[update]
# dependabot handles auto updating itself
ignored_usernames = ["dependabot"]
